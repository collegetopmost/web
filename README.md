# College Top Most
This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 19.0.6.

https://jsonplaceholder.typicode.com/

# Client Reference
https://collegevidya.com/

# For the json generation
https://developers.google.com/workspace/slides/api/reference/rest/v1/presentations/get

# For the figma
https://www.figma.com/design

# Laoder
https://cssloaders.github.io/

# past link
https://jeettechie-8314.walkthroughdemos.com/snwz/admin / jeet.techie92@gmail.com / 123456
https://jeettechie-8314.walkthroughdemos.com/snwz/admin/walkthroughs/edit2/6/8

# color picker
https://www.npmjs.com/package/ngx-color-palette?activeTab=readme
npm i ngx-color-palette

Popover 
https://www.npmjs.com/package/ngx-popover
https://www.npmjs.com/package/@ngx-popovers/popover

# for building the project 
npm build --prod
npm install @angular/cdk
ng build --configuration production --base-href /college-top-most/


# For the docs
https://docs.google.com/presentation/d/16f60GzvGk6tG3dd8V0liqkLo5MUi6BonPhM50OYs-nI/edit?slide=id.p#slide=id.p
https://docs.google.com/presentation/d/1otUqudvziUwFxHhNqMmxdmLiCeeUZg3IoNrSPgMopTM/edit?slide=id.g361d9282c36_0_1605#slide=id.g361d9282c36_0_1605

https://docs.google.com/presentation/d/176l22newLPJv8IXBN-EDk1eF95u5-RfdzYBsn319lnQ/edit?slide=id.g2dda9c719f4_0_8#slide=id.g2dda9c719f4_0_8

# basic commands
ng serve / npm start
ng new project-name
ng generate component component-name / ng g c component-name
ng build --prod
ng generate module features/your-feature --routing
ng generate module features/your-feature/your-feature-routing --flat --module=your-feature
ng generate module features/auth --routing

# Project running command
<!-- download presenatation 07-10-2025-->
npm install file-saver jszip --force            
npm install --save-dev @types/file-saver --force


# For the code generation
ng generate environments
ng generate service services/your-service
ng generate guard guards/your-guard
ng generate interface models/your-interface
ng generate pipe pipes/your-pipe
ng generate directive directives/your-directive
ng generate class shared/your-class
ng generate enum shared/your-enum
ng generate interface shared/your-interface
ng generate module shared/your-module
ng generate service shared/your-service
ng generate class shared/your-class
ng generate enum shared/your-enum
ng generate interface shared/your-interface
ng generate module shared/your-module
ng generate service shared/your-service
ng generate class shared/your-class
ng generate enum shared/your-enum

## Development server
To start a local development server, run:

```bash
ng serve
```
 # For the material [04- 05- 2025]
npm install @angular/material

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## Code scaffolding

Angular CLI includes powerful code scaffolding tools. To generate a new component, run:

```bash
ng generate component component-name
```

For a complete list of available schematics (such as `components`, `directives`, or `pipes`), run:

```bash
ng generate --help
```

## Building

To build the project run:

```bash
ng build
```

This will compile your project and store the build artifacts in the `dist/` directory. By default, the production build optimizes your application for performance and speed.

## Running unit tests

To execute unit tests with the [Karma](https://karma-runner.github.io) test runner, use the following command:

```bash
ng test
```

## Running end-to-end tests

For end-to-end (e2e) testing, run:

```bash
ng e2e
```

Angular CLI does not come with an end-to-end testing framework by default. You can choose one that suits your needs.

## Additional Resources

For more information on using the Angular CLI, including detailed command references, visit the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.
